{
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "required": true
    },
    "path": {
      "type": "string",
      "title": "Path",
      "help": {
        "description": "Full path where needs to create folder, ex: `Monthly reports/November/`, leave empty or set as \"/\" for root"
      },
      "required": false
    }
  }
}